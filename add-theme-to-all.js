// Quick script to add theme scripts to remaining pages
const pages = ['blog.html', 'notes.html', 'videos.html', 'products.html', 'gate-pyq.html', 'btech-notes.html'];

pages.forEach(page => {
    console.log(`Adding theme support to ${page}`);
    // This would be run manually to update each page
});